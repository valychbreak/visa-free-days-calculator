datasources:
  default:
    url: jdbc:h2:mem:testDb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
    dialect: H2
    maximumPoolSize: 10
    maxActive: 10

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: create